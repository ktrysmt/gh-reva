{
  "name": "gh-reva-e2e",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "cd .. && go build -o gh-reva .",
    "pretest": "cd .. && go build -o gh-reva .",
    "test": "node --test --test-force-exit --test-timeout=15000 tests/*.test.mjs",
    "test:smoke": "node --test --test-force-exit --test-timeout=15000 tests/00_smoke.test.mjs",
    "tuistory": "tuistory"
  },
  "dependencies": {
    "tuistory": "^0.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
